// preprocessor directives
#include <iostream>

int main() {
  // scope resolution operator
  std::cout << "Hello C++";
  /*
    std is a namespace. For access some thing inside the namespace
    We need use the "::" operator.
  */
  
  return 0;
}